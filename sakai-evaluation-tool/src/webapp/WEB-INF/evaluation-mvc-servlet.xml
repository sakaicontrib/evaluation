<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright 2005 Sakai Foundation Licensed under the
    Educational Community License, Version 2.0 (the "License"); you may
    not use this file except in compliance with the License. You may
    obtain a copy of the License at

    http://www.osedu.org/licenses/ECL-2.0

    Unless required by applicable law or agreed to in writing,
    software distributed under the License is distributed on an "AS IS"
    BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
    or implied. See the License for the specific language governing
    permissions and limitations under the License.

-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans   http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc     http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Scans @Controller beans in the controllers package (does not touch RSF Producers) -->
    <context:component-scan base-package="org.sakaiproject.evaluation.tool.controllers"/>

    <mvc:annotation-driven/>

    <!-- Interceptor that adds skinRepo and skinDefault to the model of every view -->
    <mvc:interceptors>
        <bean class="org.sakaiproject.evaluation.tool.controllers.SakaiSkinInterceptor"/>
    </mvc:interceptors>

    <!-- Redirect the root to summary (entry point from the Sakai portal) -->
    <mvc:view-controller path="/" view-name="redirect:/summary"/>

    <!-- Serve static resources (CSS, JS, images) from /content/ -->
    <mvc:resources mapping="/content/**" location="/content/"/>
    <!-- WebJar content (e.g. autosize) lives inside the jar's META-INF/resources/webjars/,
         not in a physical /webjars/ folder in the deployed WAR -->
    <mvc:resources mapping="/webjars/**" location="classpath:/META-INF/resources/webjars/"/>

    <!-- Thymeleaf: template resolver -->
    <bean id="mvcTemplateResolver"
          class="org.thymeleaf.spring5.templateresolver.SpringResourceTemplateResolver">
        <property name="prefix"  value="/WEB-INF/templates/"/>
        <property name="suffix"  value=".html"/>
        <property name="templateMode" value="HTML"/>
        <property name="characterEncoding" value="UTF-8"/>
        <property name="cacheable" value="false"/>
    </bean>

    <!-- Thymeleaf: template engine (reuses the messageSource from the parent context) -->
    <bean id="mvcTemplateEngine"
          class="org.thymeleaf.spring5.SpringTemplateEngine">
        <property name="enableSpringELCompiler" value="true"/>
        <property name="templateResolver" ref="mvcTemplateResolver"/>
        <property name="messageSource" ref="messageSource"/>
    </bean>

    <!-- Thymeleaf: view resolver -->
    <bean id="mvcViewResolver"
          class="org.thymeleaf.spring5.view.ThymeleafViewResolver">
        <property name="templateEngine"      ref="mvcTemplateEngine"/>
        <property name="characterEncoding"   value="UTF-8"/>
        <property name="order"               value="1"/>
    </bean>

</beans>